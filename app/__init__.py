"""app package — small, importable package root.

Keep imports lightweight to avoid import-time side-effects. Import submodules explicitly
(e.g. `from app import xor` or `from app.models import PlayerRecord`).
"""
__version__ = "0.1.0"
__author__ = "PM99 Reverse Engineering Team"
