# Legacy scripts

This directory contains analysis, research, and debug scripts that were used during the reverse engineering process of Premier Manager 99. They are preserved for reference and historical context but are no longer part of the main application. For modern tooling and scripts, see the top-level `app/scripts` and `app/cli.py`.
